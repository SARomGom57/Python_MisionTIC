# Conversion de cadena a lista
cadena = 'Colombia'
lista = list(cadena)
print(lista)

# Conversion de lista a cadena
cadena2 = ''.join(lista)
print(cadena2)

lista2 = [1,5,7,8,9]
# no se puede porque requiere que los elementos sean str

conjunto = set(lista)

diccionario = dict()

tempo = list(zip(range(len(cadena)), cadena))
print(tempo)

